
```shell

Add the .env file to the root

npm install
(if does not work;
npm install @google/generative-ai styled-components react-markdown)

#install the python libraries
pip install python-dotenv google-generativeai langchain-community fastapi pydantic uvicorn


```

# **Run**

```shell

npm start

# In a separate terminal, start the Python server
python "src/python backend/chatbot.py"

```